package slice

// Сделать 2 решения: BinarySearch реализовать самому и выквать стандартный из go библиотеки

// BinarySearch ищет target в отсортированном по возрастанию слайсе src
// методом бинарного поиска и возвращает индекс элемента, равного target.
// Если target в слайсе нет — возвращает -1.
//
// Предусловие: src отсортирован по неубыванию. Если предусловие нарушено,
// поведение не специфицировано.
//
// Сложность: O(log n) по времени, O(1) по дополнительной памяти.
//
// При наличии дубликатов допускается вернуть индекс любого из них —
// классический бинарный поиск не гарантирует «первое» или «последнее»
// вхождение.
//
// Граничные случаи:
//   - nil или пустой src -> -1;
//   - target меньше src[0] или больше src[len-1] -> -1.
func BinarySearch(src []int, target int) int {
	// TODO: реализовать бинарный поиск через два указателя lo, hi
	// и mid = lo + (hi-lo)/2 (так избегаем переполнения)
	return -1
}
