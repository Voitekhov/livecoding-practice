package channel

// ProcessConcurrently применяет fn к каждому элементу items параллельно,
// используя до workers горутин одновременно. Возвращает срез той же длины,
// что и items, причём result[i] == fn(items[i]).
//
// Гарантии:
//   - Порядок результатов соответствует порядку items (важно: сами вычисления
//     могут завершаться в произвольном порядке, но в результирующем срезе
//     порядок должен совпадать с входным).
//   - В каждый момент времени должно работать не более workers горутин,
//     обрабатывающих элементы.
//   - Для nil или пустого items — возвращает пустой слайс, горутины не
//     запускаются.
//   - Если workers <= 0, используется ровно один воркер.
//
// Подсказка по реализации:
//   1) аллоцировать result := make([]int, len(items)) — тогда воркеры пишут
//      result[idx] = fn(items[idx]) и порядок сохраняется «бесплатно»;
//   2) сделать канал заданий, в который основной горутиной отправляются
//      индексы (или пары {idx, val});
//   3) запустить workers горутин, каждая читает задания из канала через
//      for range до закрытия;
//   4) после отправки всех заданий закрыть канал заданий;
//   5) дождаться завершения всех воркеров — через sync.WaitGroup либо
//      через done-канал, в который каждый воркер пишет один раз по
//      завершении.
//
// Пример:
//
//	square := func(x int) int { return x * x }
//	ProcessConcurrently([]int{1, 2, 3, 4}, 2, square)
//	// -> [1, 4, 9, 16]
func ProcessConcurrently(items []int, workers int, fn func(int) int) []int {
	// TODO: реализовать worker pool с сохранением порядка результатов
	return nil
}
