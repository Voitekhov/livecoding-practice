package mapslice

// UniqueNames возвращает слайс той же длины, что и names, в котором каждое
// имя уникально. Имена обрабатываются по порядку слева направо:
//
//   - если имя ещё не встречалось и не «занято» — оно записывается в
//     результат как есть;
//   - если имя уже занято — к нему добавляется суффикс "(k)", где k —
//     минимальное положительное целое, при котором name(k) ещё свободно.
//     После использования name(k) считается занятым.
//
// Сравнение регистрозависимое. Для nil или пустого слайса возвращает
// пустой слайс.
//
// Пример: ["Alice", "Bob", "Alice", "Alice", "Bob"]
//
//	-> ["Alice", "Bob", "Alice(1)", "Alice(2)", "Bob(1)"]
//
// Сложный случай (суффикс уже занят на входе): ["a", "a(1)", "a"]
//
//	-> ["a", "a(1)", "a(2)"]  (a(1) уже занят, поэтому берём a(2))
func UniqueNames(names []string) []string {
	// TODO: реализовать через map[string]int (имя -> следующий k для пробы)
	// и слайс результата той же длины, что и names
	return nil
}
