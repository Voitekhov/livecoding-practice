package mapslice

// TwoSum ищет в nums два различных индекса i и j (i < j), для которых
// nums[i] + nums[j] == target, и возвращает их как []int{i, j}.
// Если такой пары нет — возвращает nil. Если возможных пар несколько,
// возвращается любая корректная.
func TwoSum(nums []int, target int) []int {
	// TODO: реализовать за O(n) через map[int]int (значение -> индекс)
	return nil
}
