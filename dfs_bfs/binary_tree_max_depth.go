package dfsbfs

// maxDepth находит максимальную глубину бинарного дерева — длину самого
// длинного пути от корня до любого листа, измеренную в количестве узлов
// (не в количестве рёбер).
//
// Формально: для узла root глубина определяется как
//   - 0, если root == nil;
//   - 1 + max(maxDepth(root.Left), maxDepth(root.Right)) иначе.
//
// Примеры:
//
//	          1
//	         / \
//	        2   3        -> 2 (корень + один из листьев)
//
//	          1
//	         / \
//	        2   3
//	       /
//	      4
//	     /
//	    5                -> 4 (1 -> 2 -> 4 -> 5)
//
// Граничные случаи:
//   - nil-дерево даёт 0;
//   - дерево из одного узла даёт 1;
//   - вырожденное дерево-«цепочка» (только Left или только Right) даёт
//     количество узлов в цепочке.
//
// Рекомендуется решать обходом в глубину (DFS), рекурсивно или со стеком.
func maxDepth(root *Node) int {
	//TODO Реализовать нахождение самой длинной ветки в бинарном дереве
	return 0
}
