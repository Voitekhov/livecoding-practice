package dfsbfs

// search ищет значение target в дереве с произвольным числом детей у
// каждого узла (MultiNode) и возвращает true, если такое значение
// встречается хотя бы один раз, иначе false.
//
// Поиск выполняется обходом в глубину (DFS): для текущего узла сначала
// проверяется его собственное значение, затем рекурсивно обходятся все
// его дети в порядке слева направо. Как только совпадение найдено,
// обход можно прекратить.
//
// Пример дерева:
//
//	          1
//	        / | \
//	       2  3  4
//	      /|     |
//	     5 6     7
//	             |
//	             8
//
// search(root, 6) == true   (узел на втором уровне)
// search(root, 8) == true   (самый глубокий лист)
// search(root, 9) == false  (значения нет в дереве)
//
// Граничные случаи:
//   - root == nil -> false;
//   - дерево из одного узла: ответ зависит от совпадения с target;
//   - у узла Children может быть nil или пустым — это валидный лист.
func search(root *MultiNode, target int) bool {
	// TODO реализовать
	return false
}
